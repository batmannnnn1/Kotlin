fun main(){

    var a = 55
    var b = 65

    // var means their value can be changed they're mutable

    println("print $a and $b ")
    println("sum of $a and $b is ${a+b}") // this is called String interpolation

    val bc = 55

    // val means their value cannot be changed because they're immutable
    // val contain the const value

    var name:String = "ayan" // you can declare the explicit variable like this



}